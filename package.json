{
  "name": "bless-anime-game",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build:debug": "tsc -p tsconfig.debug.json && bls-sdk-ts build src/main.ts -o dist -f bless-anime-game.wasm",
    "build:release": "tsc -p tsconfig.release.json && bls-sdk-ts build src/main.ts -o dist -f bless-anime-game.wasm",
    "preview": "blessnet preview serve"
  },
  "dependencies": {
    "@blockless/sdk-ts": "^1.0.5",
    "three": "^0.150.0",
    "mkdirp": "^3.0.1"
  },
  "devDependencies": {
    "@types/three": "^0.150.1",
    "typescript": "^5.0.2"
  },
  "bls": {
    "functionId": "bless-function_bless-anime-game-1.0.0"
  }
}
